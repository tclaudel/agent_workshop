---
scope: agent_workshop GitHub repo — 3-milestone educational Go+Ollama codebase
purpose: build-substrate for workshop repository
altitude: feature
updated: 2026-06-24T09:55
---

- (direction) Fast path chosen by user. Draft spine with [ASSUMPTION] tags; user corrects in review.
- (constraint) Stack fixed by PRD: Go + Ollama + llama3.2. No other runtimes, no production patterns.
- (constraint) 3 milestones required (FR-1): milestone-1 HTTP call, milestone-2 Tool struct + tools slice, milestone-3 agent loop + dispatch + history.
- (constraint) PRD Non-Goals: no error handling, retries, persistent memory, multi-agent, multi-model, production patterns in milestone code.
- (decision) AD-1 Paradigm: Progressive-Disclosure / Layered-Demonstration. Each milestone is a complete, self-contained runnable that ADDS one concept only. Prevents: a milestone that introduces two concurrent abstractions (e.g. tool struct + loop in same step).
- (decision) AD-2 Milestone isolation: each milestone lives in its own directory (milestone-1/, milestone-2/, milestone-3/) with its own main.go. No shared packages between milestones. Prevents: import-graph coupling that forces a builder to understand milestone-3 to read milestone-1.
- (decision) AD-3 Additive-only milestone delta: milestone-N+1 code is milestone-N code + exactly one new concept block. Nothing removed or restructured between milestones. Prevents: attendees losing orientation when diffing milestones side-by-side.
- (decision) AD-4 Single-file per milestone: each milestone is main.go only — no sub-packages, no internal/ dirs. Prevents: file navigation complexity that distracts from the concept being introduced.
- (decision) AD-5 Ollama HTTP API only — no Go SDK, no LLM framework. All model calls are plain http.Post to http://localhost:11434/api/chat. Prevents: one builder using the ollama-go client library, another using raw HTTP — non-comparable code in the live session.
- (decision) AD-6 Tool schema: Tool struct has exactly 4 fields: Name string, Description string, Parameters json.RawMessage, Run func(args map[string]any) string. Prevents: milestone-2 and milestone-3 using divergent Tool shapes.
- (decision) AD-7 Message history: history is []Message accumulated in milestone-3 loop — never truncated, never persisted to disk. Prevents: one builder adding rolling-window, another adding file persistence (both out of PRD scope).
- (decision) AD-8 Stop condition: loop exits when model response contains no tool_calls field (plain text reply). Prevents: alternative stops (max-turns, error, timeout) being introduced — PRD explicitly defers stop condition robustness to post-workshop extension questions.
- (decision) AD-9 Reference binary: compiled from milestone-3/main.go for darwin/arm64 (Thomas's machine). Committed as agent-demo binary at repo root. No multi-arch. Prevents: binary built from a different source than the milestone code.
- (decision) AD-10 Go module: single go.mod at repo root covering all milestones and starter. Module path: github.com/tclaudel/agent-workshop [ASSUMPTION]. stdlib only — no external dependencies. Prevents: per-milestone go.mod files that complicate go build invocations for attendees.
- (assumption) Module path assumed github.com/tclaudel/agent-workshop — confirm with Thomas before first build.
- (assumption) Thomas's facilitation machine is darwin/arm64 (Apple Silicon) — determines reference binary target.
- (assumption) llama3.2 (3B) is the target model — supports tool_calls in Ollama chat API as of Ollama 0.3+.
- (question) Does the demo task file (used in FR-3 binary demo) live at repo root or in a demo/ subdir? Affects AD-9 and repo tree.
- (question) Is starter/main.go a directory (starter/main.go) or a flat file at root (starter.go)? PRD says starter/ directory — confirm.
- (event) Reviewer Gate complete: lint 0 findings; rubric PASS; tech-versions CRITICAL (Ollama/llama3.2 versions unverified — flagged in Stack); adversarial INCOMPATIBLE (5 holes — 4 new ADs added AD-10/11/12/13).
- (decision) AD-10 Message struct shape: Role string, Content string, ToolCalls []ToolCall. Prevents incompatible field naming between independent builders.
- (decision) AD-11 Ollama envelope typed structs (ChatRequest, ToolDef, ToolFunction, ChatResponse, ToolCall, ToolCallFunction). Prevents map[string]any vs typed-struct divergence.
- (decision) AD-12 Tool dispatch uses switch on msg.ToolCalls[0].Function.Name. Prevents switch/if-else/map-dispatch structural divergence.
- (decision) AD-13 HTTP calls use http.Post shorthand, no custom http.Client. Prevents milestone-1 import/structure divergence.
- (event) spine finalized
